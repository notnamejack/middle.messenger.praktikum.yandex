<div class="input">
    <label for="{{id}}">{{label}}</label>
    <input
        ref="input-${{id}}"
        id="{{id}}" 
        name="{{name}}"
        type="{{type}}"
        value="{{value}}"
    /> 
</div>
