<div class="input-horizontal">
    <label for="{{id}}">{{label}}</label>
    <input 
        ref="input-horizontal-${{id}}"
        id="{{id}}" 
        name="{{name}}"
        type="{{type}}"
        value="{{value}}"
    /> 
</div>
